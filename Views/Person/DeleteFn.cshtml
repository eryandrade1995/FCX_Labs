@model Person;
@{
    ViewData["Title"] = "Apagar Pessoa?";
}
<div class="row">
    <div class="col-md-12" id="login-wraper" style="background: none repeat scroll 0 0 rgba(100%, 100%, 100%, 0.4);">
        <div class="text-center">
            <h1>Deseja realmente apagar @Model.name?</h1>
        </div>

        <a class="btn btn-primary" role="button" asp-controller="Person" asp-action="Index">Não</a>
        <a class="btn btn-danger" role="button" asp-controller="Person" asp-route-id="@Model.id"
            asp-action="Delete">Sim</a>
    </div>
</div>